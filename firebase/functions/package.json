{
  "name": "seo-mcp-server-functions",
  "version": "1.0.0",
  "description": "Firebase backend for seo-mcp-server",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
